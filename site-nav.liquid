{% comment %}
  List out your main-menu linklist (default)

  More info on linklists:
    - http://docs.shopify.com/themes/liquid-variables/linklists
{% endcomment %}
<ul class="site-nav" id="AccessibleNav">
  {% assign page_id = page.id %}
  {% if page_id != 111115698331 %}
    <style>

.black-friday-sale-menu-link-desktop {
  background: linear-gradient(45deg, #0071ce 0%, #dc3545 50%, #0071ce 100%);
  background-size: 300% 300%; /* Larger than the element to enable movement */
  color: #ffffff;
  padding: 10px 5px;
  font-weight: 500;
  font-size: 14px;
  border-radius: 10px;
  letter-spacing: 0.5px;
  text-align: center;
  display: inline-block;
  animation: movingBackground 5s linear infinite; /* Add background animation */
  text-decoration: none; /* Ensure the link looks clean */
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
}


a.black-friday-sale-menu-link-desktop:hover, a.black-friday-sale-menu-link-desktop:active {
  color:#ffff!important
}
@keyframes movingBackground {
  0% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
  100% {
    background-position: 0% 50%; 
  }
}
    </style>
<a href="/pages/ezbio-lab-essentials-sale-up-to-25-off-centrifuge-vacuum-assemblies" target="_blank" follow="no-follow" class="black-friday-sale-menu-link-desktop ">Foxx Summer Sale</a>

<style>
/* Foxx Bioprocess 2030 Design Highlight */
.foxx-bioprocess-highlight {
  position: relative;
  margin: 0 10px;
}

.foxx-bioprocess-link {
  position: relative;
  display: flex !important;
  align-items: center;
  gap: 8px;
  padding: 12px 20px !important;
  background: linear-gradient(135deg, #0071ce 0%, #003863 50%, #0071ce 100%);
  background-size: 200% 200%;
  border-radius: 25px;
  color: #ffffff !important;
  text-decoration: none;
  font-weight: 600;
  font-size: 14px;
  letter-spacing: 0.5px;
  transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  overflow: hidden;
  box-shadow: 
    0 8px 25px rgba(0, 113, 206, 0.3),
    0 0 0 1px rgba(255, 255, 255, 0.1) inset;
  animation: gradientShift 4s ease-in-out infinite;
  border: 2px solid transparent;
  background-clip: padding-box;
}

.foxx-bioprocess-link::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(45deg, transparent 30%, rgba(255, 255, 255, 0.1) 50%, transparent 70%);
  transform: translateX(-100%);
  transition: transform 0.6s;
  z-index: 1;
}

.foxx-bioprocess-link:hover::before {
  transform: translateX(100%);
}

.foxx-bioprocess-link:hover {
  background: linear-gradient(135deg, #0071ce 0%, #005bb5 50%, #003863 100%);
  transform: translateY(-2px) scale(1.05);
  box-shadow: 
    0 15px 35px rgba(0, 113, 206, 0.4),
    0 5px 15px rgba(0, 56, 99, 0.3),
    0 0 0 1px rgba(255, 255, 255, 0.2) inset;
  color: #ffffff !important;
}

.foxx-bioprocess-icon {
  font-size: 16px;
  z-index: 2;
  position: relative;
  animation: rocketPulse 2s ease-in-out infinite;
}

.foxx-bioprocess-text {
  z-index: 2;
  position: relative;
  font-weight: 600;
  text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
}

.foxx-bioprocess-badge {
  position: absolute;
  top: -8px;
  right: -8px;
  background: linear-gradient(45deg, #ff6b35, #f7931e);
  color: white;
  font-size: 10px;
  font-weight: 700;
  padding: 2px 6px;
  border-radius: 10px;
  z-index: 3;
  box-shadow: 0 2px 8px rgba(255, 107, 53, 0.4);
  animation: badgePulse 2s ease-in-out infinite alternate;
}

/* Glow effect for extra attention */
.foxx-bioprocess-link::after {
  content: '';
  position: absolute;
  top: -2px;
  left: -2px;
  right: -2px;
  bottom: -2px;
  background: linear-gradient(45deg, #0071ce, #003863, #0071ce);
  border-radius: 27px;
  z-index: -1;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.foxx-bioprocess-link:hover::after {
  opacity: 0.7;
  animation: glowPulse 1.5s ease-in-out infinite;
}

/* Keyframe animations */
@keyframes gradientShift {
  0%, 100% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
}

@keyframes rocketPulse {
  0%, 100% {
    transform: scale(1);
  }
  50% {
    transform: scale(1.1);
  }
}

@keyframes badgePulse {
  0% {
    transform: scale(1);
    box-shadow: 0 2px 8px rgba(255, 107, 53, 0.4);
  }
  100% {
    transform: scale(1.1);
    box-shadow: 0 4px 12px rgba(255, 107, 53, 0.6);
  }
}

@keyframes glowPulse {
  0%, 100% {
    opacity: 0.7;
  }
  50% {
    opacity: 1;
  }
}

/* Responsive design */
@media (max-width: 768px) {
  .foxx-bioprocess-link {
    padding: 10px 16px !important;
    font-size: 13px;
    border-radius: 20px;
  }
  
  .foxx-bioprocess-icon {
    font-size: 14px;
  }
  
  .foxx-bioprocess-badge {
    font-size: 9px;
    padding: 1px 4px;
  }
}

/* Dark mode compatibility */
@media (prefers-color-scheme: dark) {
  .foxx-bioprocess-link {
    background: linear-gradient(135deg, #0071ce 0%, #001a2e 50%, #0071ce 100%);
    box-shadow: 
      0 8px 25px rgba(0, 113, 206, 0.4),
      0 0 0 1px rgba(255, 255, 255, 0.15) inset;
  }
}

/* Enhanced focus for accessibility */
.foxx-bioprocess-link:focus {
  outline: 3px solid rgba(0, 113, 206, 0.5);
  outline-offset: 2px;
}

/* Futuristic glass morphism effect */
.foxx-bioprocess-highlight::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 25px;
  backdrop-filter: blur(10px);
  z-index: -2;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.foxx-bioprocess-highlight:hover::before {
  opacity: 1;
}
</style> 

  {% endif %}  
 {% comment %} <a href="/pages/us-independence-day-sale-25" style="
   background: linear-gradient(45deg, #0071ce 0%, #dc3545 50%, #0071ce 100%);
    color: #ffffff;
    padding-inline: 10px;
    font-weight: 500!important;
    border-radius:10px;
    letter-spacing: 0.5px;position: relative;
    padding: 10px 10px;">Biggest Patriotic Sale</a>  {% endcomment %}
  {% for link in linklists.new-main-menu.links %}
    {% comment %}
      Create a dropdown menu by naming a linklist the same as a link in the parent nav

      More info on dropdowns:
        - http://docs.shopify.com/manual/your-website/navigation/create-drop-down-menu
    {% endcomment %}
    {% assign child_list_handle = link.title | handleize %}
    {% if linklists[child_list_handle].links != blank %}
      <li class="site-nav--has-dropdown{% if link.active %} site-nav--active{% endif %}" aria-haspopup="true">
        <a href="{{ link.url }}" class="site-nav__link">
          {{ link.title }}
          <span class="icon-fallback-text">
            <span class="icon icon-arrow-down" aria-hidden="true"></span>
          </span>
        </a>
        <ul class="site-nav__dropdown">
          {% for childlink in linklists[child_list_handle].links %}
            <li{% if childlink.active %} class="site-nav--active"{% endif %}>
              <a href="{{ childlink.url }}" class="site-nav__link">{{ childlink.title | escape }}</a>
            </li>
          {% endfor %}
        </ul>
      </li>
    {% else %}
      <li {% if link.active %}class="site-nav--active"{% endif %}{% if link.url contains 'foxxbioprocess.com' %} class="foxx-bioprocess-highlight"{% endif %}>
        <a href="{{ link.url }}" class="site-nav__link{% if link.url contains 'foxxbioprocess.com' %} foxx-bioprocess-link{% endif %}"{% if link.url contains 'foxxbioprocess.com' %} target="_blank"{% endif %}>
          {% if link.url contains 'foxxbioprocess.com' %}
            <span class="foxx-bioprocess-icon">🚀</span>
            <span class="foxx-bioprocess-text">{{ link.title }}</span>
            <span class="foxx-bioprocess-badge">NEW</span>
          {% else %}
            {{ link.title }}
          {% endif %}
        </a>
      </li>
    {% endif %}
  {% endfor %}
</ul>
