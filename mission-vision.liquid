{% schema %}
{
  "name": "Mission & Vision",
  "class": "mission-vision-section",
  "settings": [
    {
      "type": "header",
      "content": "General Settings"
    },
    {
      "type": "checkbox",
      "id": "show_section",
      "label": "Show Section",
      "default": true
    },
    {
      "type": "text",
      "id": "section_title",
      "label": "Section Title",
      "default": "Our Mission & Vision"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#f8f9fa"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent Color",
      "default": "#0071b9"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top Padding",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom Padding",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px", 
      "default": 80
    }
  ],
  "blocks": [
    {
      "type": "mission_vision_card",
      "name": "Mission/Vision Card",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Card Title",
          "default": "Mission"
        },
        {
          "type": "textarea",
          "id": "content",
          "label": "Card Content",
          "default": "To enable researchers, biotech labs, and bioprocess professionals to receive custom components that meet global quality standards."
        },
        {
          "type": "select",
          "id": "icon_type",
          "label": "Icon Type",
          "default": "svg",
          "options": [
            {
              "value": "svg",
              "label": "SVG Icon"
            },
            {
              "value": "image",
              "label": "Custom Image"
            }
          ]
        },
        {
          "type": "textarea",
          "id": "svg_code",
          "label": "SVG Code",
          "default": "<svg viewBox='0 0 24 24' fill='currentColor'><path d='M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z'/></svg>"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Icon Image"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Mission & Vision",
      "category": "About",
      "blocks": [
        {
          "type": "mission_vision_card",
          "settings": {
            "title": "Mission",
            "content": "To enable researchers, biotech labs, and bioprocess professionals to receive custom components that meet global quality standards. We focus on sustainable, sterile, and scalable manufacturing that adapts to user-defined designs across SUT, filtration, fluid transfer, and storage systems.",
            "icon_type": "svg",
            "svg_code": "<svg viewBox='0 0 24 24' fill='currentColor'><circle cx='12' cy='12' r='10' fill='none' stroke='currentColor' stroke-width='2'/><circle cx='12' cy='12' r='6' fill='none' stroke='currentColor' stroke-width='2'/><circle cx='12' cy='12' r='2'/></svg>"
          }
        },
        {
          "type": "mission_vision_card",
          "settings": {
            "title": "Vision",
            "content": "To become the global benchmark for custom & made-to-order bioprocessing solutions by combining technical design, cleanroom assembly, and regulatory assurance into one seamless digital-to-physical workflow.",
            "icon_type": "svg",
            "svg_code": "<svg viewBox='0 0 24 24' fill='currentColor'><path d='M12 4.5C7 4.5 2.73 7.61 1 12c1.73 4.39 6 7.5 11 7.5s9.27-3.11 11-7.5c-1.73-4.39-6-7.5-11-7.5zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z'/></svg>"
          }
        }
      ]
    }
  ]
}
{% endschema %}

{% if section.settings.show_section %}
<!-- Mission & Vision Section -->
<section class="mv-professional-mission-vision" data-section-id="{{ section.id }}" style="background: linear-gradient(135deg, {{ section.settings.background_color | default: '#f8f9fa' }} 0%, rgba(0, 113, 185, 0.05) 100%); padding: {{ section.settings.padding_top | default: 80 }}px 0 {{ section.settings.padding_bottom | default: 80 }}px 0; width: 100vw; margin-left: calc(-50vw + 50%);">
  <div class="mv-mission-vision-container">
    {% if section.settings.section_title != blank %}
      <div class="mv-section-header">
        <div class="mv-title-decoration">
          <span class="mv-decoration-line"></span>
          <span class="mv-decoration-diamond"></span>
          <span class="mv-decoration-line"></span>
        </div>
        <h2 class="mv-section-title">{{ section.settings.section_title }}</h2>
        <p class="mv-section-subtitle">Driving Innovation in Bioprocess Solutions</p>
      </div>
    {% endif %}

    <div class="mv-mission-vision-grid">
      {% if section.blocks.size > 0 %}
        {% for block in section.blocks %}
          <div class="mv-professional-card" data-aos="fade-up" data-aos-delay="{{ forloop.index0 | times: 200 }}" {{ block.shopify_attributes }}>
            <div class="mv-card-background-effect"></div>

            <div class="mv-card-header">
              <div class="mv-icon-container">
                {% if block.settings.icon_type == 'image' and block.settings.image != blank %}
                  <div class="mv-card-icon">
                    <img src="{{ block.settings.image | img_url: '60x60' }}" alt="{{ block.settings.title }}" loading="lazy">
                  </div>
                {% elsif block.settings.svg_code != blank %}
                  <div class="mv-card-icon svg-icon" style="color: {{ section.settings.accent_color | default: '#0071b9' }};">
                    {{ block.settings.svg_code }}
                  </div>
                {% endif %}
                <div class="mv-icon-glow"></div>
              </div>

              <h3 class="mv-card-title" style="color: {{ section.settings.accent_color | default: '#0071b9' }};">
                {{ block.settings.title | default: 'Title' }}
              </h3>
            </div>

            <div class="mv-card-content">
              <p class="mv-card-description">{{ block.settings.content }}</p>
            </div>

            <div class="mv-card-footer">
              <div class="mv-progress-indicator">
                <div class="mv-progress-bar" style="background: linear-gradient(90deg, {{ section.settings.accent_color | default: '#0071b9' }}, {{ section.settings.accent_color | default: '#0071b9' | color_lighten: 20 }});"></div>
              </div>
            </div>
          </div>
        {% endfor %}
      {% else %}
        <!-- Default Mission Card -->
        <div class="mv-professional-card" data-aos="fade-up" data-aos-delay="0">
          <div class="mv-card-background-effect"></div>

          <div class="mv-card-header">
            <div class="mv-icon-container">
              <div class="mv-card-icon svg-icon" style="color: {{ section.settings.accent_color | default: '#0071b9' }};">
                <svg viewBox='0 0 24 24' fill='currentColor'><circle cx='12' cy='12' r='10' fill='none' stroke='currentColor' stroke-width='2'/><circle cx='12' cy='12' r='6' fill='none' stroke='currentColor' stroke-width='2'/><circle cx='12' cy='12' r='2'/></svg>
              </div>
              <div class="mv-icon-glow"></div>
            </div>

            <h3 class="mv-card-title" style="color: {{ section.settings.accent_color | default: '#0071b9' }};">
              Mission
            </h3>
          </div>

          <div class="mv-card-content">
            <p class="mv-card-description">To enable researchers, biotech labs, and bioprocess professionals to receive custom components that meet global quality standards. We focus on sustainable, sterile, and scalable manufacturing that adapts to user-defined designs across SUT, filtration, fluid transfer, and storage systems.</p>
          </div>

          <div class="mv-card-footer">
            <div class="mv-progress-indicator">
              <div class="mv-progress-bar" style="background: linear-gradient(90deg, {{ section.settings.accent_color | default: '#0071b9' }}, {{ section.settings.accent_color | default: '#0071b9' | color_lighten: 20 }});"></div>
            </div>
          </div>
        </div>

        <!-- Default Vision Card -->
        <div class="mv-professional-card" data-aos="fade-up" data-aos-delay="200">
          <div class="mv-card-background-effect"></div>

          <div class="mv-card-header">
            <div class="mv-icon-container">
              <div class="mv-card-icon svg-icon" style="color: {{ section.settings.accent_color | default: '#0071b9' }};">
                <svg viewBox='0 0 24 24' fill='currentColor'><path d='M12 4.5C7 4.5 2.73 7.61 1 12c1.73 4.39 6 7.5 11 7.5s9.27-3.11 11-7.5c-1.73-4.39-6-7.5-11-7.5zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z'/></svg>
              </div>
              <div class="mv-icon-glow"></div>
            </div>

            <h3 class="mv-card-title" style="color: {{ section.settings.accent_color | default: '#0071b9' }};">
              Vision
            </h3>
          </div>

          <div class="mv-card-content">
            <p class="mv-card-description">To become the global benchmark for custom & made-to-order bioprocessing solutions by combining technical design, cleanroom assembly, and regulatory assurance into one seamless digital-to-physical workflow.</p>
          </div>

          <div class="mv-card-footer">
            <div class="mv-progress-indicator">
              <div class="mv-progress-bar" style="background: linear-gradient(90deg, {{ section.settings.accent_color | default: '#0071b9' }}, {{ section.settings.accent_color | default: '#0071b9' | color_lighten: 20 }});"></div>
            </div>
          </div>
        </div>
      {% endif %}
    </div>
  </div>
</section>
{% endif %}

<style>
/* Mission Vision Section - Unique CSS Classes */
.mv-professional-mission-vision {
  position: relative;
  overflow: hidden;
  width: 100%;
}

.mv-professional-mission-vision::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%230071b9' fill-opacity='0.03'%3E%3Ccircle cx='30' cy='30' r='2'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
  pointer-events: none;
}

.mv-mission-vision-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 20px;
  position: relative;
  z-index: 2;
}

.mv-professional-mission-vision {
  margin: 0 !important;
}

/* Enhanced Section Header */
.mv-section-header {
  text-align: center;
  margin-bottom: 80px;
  position: relative;
}

.mv-title-decoration {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 20px;
  gap: 15px;
}

.mv-decoration-line {
  width: 60px;
  height: 2px;
  background: linear-gradient(90deg, transparent, #0071b9, transparent);
}

.mv-decoration-diamond {
  width: 12px;
  height: 12px;
  background: #0071b9;
  transform: rotate(45deg);
  border-radius: 2px;
}

.mv-section-title {
  font-size: 48px;
  font-weight: 800;
  color: #0071b9;
  margin-bottom: 20px;
  text-transform: uppercase;
  letter-spacing: 4px;
  text-shadow: 0 2px 8px rgba(0, 113, 185, 0.3);
  position: relative;
  display: inline-block;
  line-height: 1.2;
}

.mv-section-title::before {
  content: '';
  position: absolute;
  bottom: -10px;
  left: 50%;
  transform: translateX(-50%);
  width: 100px;
  height: 4px;
  background: linear-gradient(90deg, transparent, #0071b9, transparent);
  border-radius: 2px;
}

.mv-section-title::after {
  content: '';
  position: absolute;
  bottom: -20px;
  left: 50%;
  transform: translateX(-50%);
  width: 200px;
  height: 2px;
  background: linear-gradient(90deg, transparent, rgba(0, 113, 185, 0.5), transparent);
  border-radius: 1px;
}

.mv-section-subtitle {
  font-size: 20px;
  color: rgba(0, 113, 185, 0.8);
  line-height: 1.6;
  max-width: 600px;
  margin: 0 auto;
  font-weight: 300;
  text-shadow: 0 1px 4px rgba(0, 113, 185, 0.2);
  letter-spacing: 1px;
  font-style: italic;
}

/* Mission Vision Grid */
.mv-mission-vision-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
  gap: 40px;
  align-items: stretch;
}

/* Mission Vision Cards */
.mv-professional-card {
  background: white;
  border-radius: 24px;
  padding: 45px 35px;
  position: relative;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  overflow: hidden;
  box-shadow: 0 20px 60px rgba(0, 113, 185, 0.08);
  border: 1px solid rgba(0, 113, 185, 0.1);
  min-height: 380px;
  display: flex;
  flex-direction: column;
}

.mv-card-background-effect {
  position: absolute;
  top: -50%;
  right: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(circle, rgba(0, 113, 185, 0.02) 0%, transparent 70%);
  transform: scale(0);
  transition: transform 0.6s ease;
  pointer-events: none;
}

.mv-professional-card:hover .mv-card-background-effect {
  transform: scale(1);
}

.mv-professional-card:hover {
  transform: translateY(-8px);
  box-shadow: 0 30px 80px rgba(0, 113, 185, 0.15);
  border-color: rgba(0, 113, 185, 0.2);
}

/* Mission Vision Card Header */
.mv-card-header {
  text-align: center;
  margin-bottom: 30px;
  position: relative;
}

.mv-icon-container {
  position: relative;
  display: inline-block;
  margin-bottom: 25px;
}

.mv-card-icon {
  width: 80px;
  height: 80px;
  background: linear-gradient(135deg, rgba(0, 113, 185, 0.1), rgba(0, 113, 185, 0.2));
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  z-index: 2;
  transition: all 0.4s ease;
  border: 3px solid rgba(0, 113, 185, 0.1);
}

.mv-card-icon svg {
  width: 40px;
  height: 40px;
  transition: transform 0.3s ease;
}

.mv-card-icon img {
  width: 40px;
  height: 40px;
  object-fit: contain;
  transition: transform 0.3s ease;
}

.mv-icon-glow {
  position: absolute;
  top: -10px;
  left: -10px;
  right: -10px;
  bottom: -10px;
  background: linear-gradient(45deg, #0071b9, #005a91);
  border-radius: 50%;
  opacity: 0;
  transition: opacity 0.3s ease;
  z-index: 1;
  filter: blur(15px);
}

.mv-professional-card:hover .mv-icon-glow {
  opacity: 0.3;
}

.mv-professional-card:hover .mv-card-icon {
  transform: scale(1.1);
  background: linear-gradient(135deg, rgba(0, 113, 185, 0.2), rgba(0, 113, 185, 0.3));
  border-color: rgba(0, 113, 185, 0.3);
}

.mv-professional-card:hover .mv-card-icon svg,
.mv-professional-card:hover .mv-card-icon img {
  transform: scale(1.1);
}

.mv-card-title {
  font-size: 28px;
  font-weight: 700;
  margin: 0;
  text-transform: uppercase;
  letter-spacing: 2px;
  position: relative;
}

.mv-card-title::after {
  content: '';
  position: absolute;
  bottom: -8px;
  left: 50%;
  transform: translateX(-50%);
  width: 40px;
  height: 3px;
  background: linear-gradient(90deg, #0071b9, #005a91);
  border-radius: 2px;
  transition: width 0.3s ease;
}

.mv-professional-card:hover .mv-card-title::after {
  width: 60px;
}

/* Mission Vision Card Content */
.mv-card-content {
  flex: 1;
  display: flex;
  align-items: center;
}

.mv-card-description {
  font-size: 16px;
  color: #555;
  line-height: 28px;
  margin: 0;
  text-align: center;
  font-weight: 400;
  transition: color 0.3s ease;
}

.mv-professional-card:hover .mv-card-description {
  color: #333;
}

/* Mission Vision Card Footer */
.mv-card-footer {
  margin-top: 30px;
}

.mv-progress-indicator {
  width: 100%;
  height: 4px;
  background: rgba(0, 113, 185, 0.1);
  border-radius: 2px;
  overflow: hidden;
}

.mv-progress-bar {
  height: 100%;
  width: 0%;
  border-radius: 2px;
  transition: width 2s ease;
}

.mv-professional-card:hover .mv-progress-bar {
  width: 100%;
}

/* Mission Vision Responsive Design */
@media (max-width: 768px) {
  .mv-mission-vision-container {
    padding: 0 15px;
  }

  .mv-section-title {
    font-size: 36px;
    letter-spacing: 2px;
    line-height: 1.2;
  }

  .mv-section-title::before {
    width: 80px;
    height: 3px;
  }

  .mv-section-title::after {
    width: 150px;
  }

  .mv-section-subtitle {
    font-size: 18px;
  }

  .mv-mission-vision-grid {
    grid-template-columns: 1fr;
    gap: 30px;
  }

  .mv-professional-card {
    padding: 35px 25px;
    min-height: 320px;
  }

  .mv-card-icon {
    width: 70px;
    height: 70px;
  }

  .mv-card-icon svg,
  .mv-card-icon img {
    width: 35px;
    height: 35px;
  }

  .mv-card-title {
    font-size: 24px;
    letter-spacing: 1px;
  }

  .mv-card-description {
    font-size: 15px;
    line-height: 24px;
  }

  .mv-decoration-line {
    width: 40px;
  }

  .mv-title-decoration {
    gap: 10px;
  }
}

@media (max-width: 480px) {
  .mv-section-title {
    font-size: 28px;
    line-height: 1.2;
    letter-spacing: 2px;
  }

  .mv-section-title::before {
    width: 60px;
    height: 2px;
  }

  .mv-section-title::after {
    width: 120px;
  }

  .mv-section-subtitle {
    font-size: 16px;
  }

  .mv-professional-card {
    padding: 30px 20px;
  }

  .mv-card-title {
    font-size: 22px;
  }

  .mv-card-description {
    font-size: 14px;
    line-height: 22px;
  }
}

/* Mission Vision Animation on Scroll */
@media (prefers-reduced-motion: no-preference) {
  .mv-professional-card {
    opacity: 0;
    transform: translateY(30px);
    animation: mvFadeInUp 0.8s ease forwards;
  }

  .mv-professional-card:nth-child(2) {
    animation-delay: 0.2s;
  }

  .mv-professional-card:nth-child(3) {
    animation-delay: 0.4s;
  }

  @keyframes mvFadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
}

/* Mission Vision Accessibility */
@media (prefers-reduced-motion: reduce) {
  .mv-professional-card,
  .mv-card-icon,
  .mv-progress-bar {
    animation: none;
    transition: none;
  }
}

.mv-professional-card:focus-within {
  outline: 3px solid #0071b9;
  outline-offset: 4px;
}

/* Enhanced Mission Vision Professional Touch */
.mv-professional-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(90deg, #0071b9, #005a91, #0071b9);
  background-size: 200% 100%;
  animation: mvShimmer 3s ease-in-out infinite;
}

@keyframes mvShimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}
</style>