


<style>
  .homepage-slider { 
    display: block !important; 
    border: 2px solid transparent; 
    background-image: linear-gradient(white, white), linear-gradient(to bottom, #0071ce, #5da8e5, #9dc8ec); 
    background-origin: border-box; 
    background-clip: content-box, border-box; 
    border-radius: 12px;
    opacity: 0;
    transition: opacity 0.3s ease;
    height: 600px;
    overflow: hidden;
    position: relative;
  }

  .homepage-slider.slick-initialized {
    opacity: 1;
  }

  .homepage-slider img { 
    border-radius: 12px; 
  }

  /* Hide slides initially until slider is initialized */
  .homepage-slider:not(.slick-initialized) .slide-item {
    display: none;
  }

  .homepage-slider:not(.slick-initialized) .slide-item:first-child {
    display: block;
  }
</style>

<section class="homepage-slider">
  {% comment %} Show slide 3 first {% endcomment %}
  {% assign slide_order = '3,1,2,4,5,6,7,8' | split: ',' %}
  {% for slide_num in slide_order %}
    {% assign i = slide_num | plus: 0 %}
    {% capture slide %}slide_{{ i }}{% endcapture %}
    {% capture slide_link %}slide_{{ i }}_link{% endcapture %}
    {% capture slide_img %}slide_{{ i }}.jpg{% endcapture %}
    {% capture slide_alt %}slide_{{ i }}_alt{% endcapture %}
    {% capture slide_is_video %}slide_{{ i }}_is_video{% endcapture %}
    {% capture slide_video_url %}slide_{{ i }}_video_url{% endcapture %}

    {% if settings[slide] %}
      <div class="slide-item {% if settings[slide_is_video] and settings[slide_video_url] != blank %}video-slide{% else %}image-slide{% endif %}" data-slide-index="{{ forloop.index0 }}">
        <a href="{{ settings[slide_link] }}" class="slide-link">
          {% if settings[slide_is_video] and settings[slide_video_url] != blank %}
            <div class="video-container">
              <video 
                class="slide-video" 
                muted 
                playsinline
                preload="metadata"
                src="{{ settings[slide_video_url] }}"
              >
                <source src="{{ settings[slide_video_url] }}" type="video/mp4">
                Your browser does not support the video tag.
              </video>
            </div>
          {% else %}
            <div class="image-container">
              {{ slide_img | asset_url | img_tag: settings[slide_alt] }}
            </div>
          {% endif %}
        </a>
      </div>
    {% endif %}
  {% endfor %}
</section>

<style>
/* Base slider styling */
.homepage-slider {
  height: 600px !important;
  min-height: 600px;
  max-height: 600px;
  overflow: hidden;
  position: relative;
  background: #000;
  width: 100%;
}

@media screen and (max-width: 768px) {
  .homepage-slider {
    height: 400px !important;
    min-height: 400px;
    max-height: 400px;
  }
}

/* Slide container styling */
.slide-item {
  position: relative;
  width: 100%;
  height: 600px !important;
  overflow: hidden;
  display: flex !important;
  align-items: center;
  justify-content: center;
  background: #000;
}

@media screen and (max-width: 768px) {
  .slide-item {
    height: 400px !important;
  }
}

/* Video container and styling - FIXED FOR PROPER SIZING */
.video-container {
  position: relative;
  width: 100%;
  height: 100%;
  background: #000;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  border-radius: 12px;
}

.slide-video {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 100%;
  height: 100%;
  min-width: 100%;
  min-height: 100%;
  object-fit: cover;
  object-position: center;
  transform: translate(-50%, -50%);
  background: #000;
  border-radius: 12px;
  z-index: 1;
}

/* Ensure video fills the entire container */
@media screen and (max-width: 768px) {
  .slide-video {
    width: 100% !important;
    height: 100% !important;
  }
}

/* Image container and styling */
.image-container {
  width: 100%;
  height: 100%;
  position: relative;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #f5f5f5;
}

.image-container img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  object-position: center;
  display: block;
  background: #f5f5f5;
  border-radius: 12px;
}

/* Slide link styling */
.slide-link {
  display: block;
  width: 100%;
  height: 100%;
  position: relative;
  overflow: hidden;
}

/* Slick slider overrides */
.homepage-slider .slick-track {
  display: flex !important;
  align-items: stretch;
  height: 600px !important;
}

@media screen and (max-width: 768px) {
  .homepage-slider .slick-track {
    height: 400px !important;
  }
}

.homepage-slider .slick-slide {
  height: 600px !important;
  display: flex !important;
  align-items: center;
  overflow: hidden;
}

@media screen and (max-width: 768px) {
  .homepage-slider .slick-slide {
    height: 400px !important;
  }
}

.homepage-slider .slick-slide > div {
  width: 100%;
  height: 100%;
  overflow: hidden;
}

/* Slider controls styling */
.homepage-slider .slick-dots {
  bottom: 20px;
  z-index: 10;
  position: absolute;
}

.homepage-slider .slick-prev,
.homepage-slider .slick-next {
  z-index: 10;
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
}

.homepage-slider .slick-prev {
  left: 20px;
}

.homepage-slider .slick-next {
  right: 20px;
}

/* Style the dots */
.homepage-slider .slick-dots li button {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.5);
  border: 2px solid rgba(255, 255, 255, 0.8);
}

.homepage-slider .slick-dots li.slick-active button {
  background: rgba(255, 255, 255, 1);
}

/* Style the arrows */
.homepage-slider .slick-prev:before,
.homepage-slider .slick-next:before {
  color: rgba(255, 255, 255, 0.9);
  font-size: 30px;
}

.homepage-slider .slick-prev:hover:before,
.homepage-slider .slick-next:hover:before {
  color: rgba(255, 255, 255, 1);
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  let sliderInitialized = false;

  function stopAllVideos() {
    document.querySelectorAll('.slide-video').forEach(video => {
      try {
        video.pause();
        video.currentTime = 0;
      } catch (e) {
        console.log('Error stopping video:', e);
      }
    });
  }

  function playCurrentVideo(currentSlide) {
    const currentSlideElement = document.querySelector(`.slick-slide[data-slick-index="${currentSlide}"]`);
    if (!currentSlideElement) return;

    const video = currentSlideElement.querySelector('video');
    if (video) {
      console.log('Playing video on slide', currentSlide);

      // Force reload the video to ensure it's properly loaded
      video.load();
      video.muted = true;
      
      // Enhanced event listeners to prevent pausing
      video.addEventListener('pause', function(e) {
        if (!video.ended && !video.seeking) {
          setTimeout(() => {
            video.play().catch(err => console.log('Resume after pause failed:', err));
          }, 100);
        }
      });

      // Prevent browser from pausing on visibility change
      video.addEventListener('webkitbeginfullscreen', function(e) {
        e.preventDefault();
      });

      // Wait for video to be ready before playing
      const attemptPlay = () => {
        if (video.readyState >= 2) {
          video.currentTime = 0;
          video.play().catch(e => {
            console.log('Video play failed, retrying...', e);
            setTimeout(() => {
              video.load();
              video.currentTime = 0;
              video.play().catch(err => console.log('Second play attempt failed:', err));
            }, 500);
          });
        } else {
          video.addEventListener('loadeddata', () => {
            video.currentTime = 0;
            video.play().catch(e => console.log('Video play failed after loadeddata:', e));
          }, { once: true });
        }
      };

      attemptPlay();
    }
  }

  function initializeSlider() {
    if (sliderInitialized || typeof $ === 'undefined' || !$.fn.slick) return;

    if ($('.homepage-slider').hasClass('slick-initialized')) {
      $('.homepage-slider').slick('unslick');
    }

    // Determine initial autoplay speed based on first slide
    const firstSlideElement = document.querySelector('.slide-item:first-child');
    const isFirstSlideVideo = firstSlideElement && firstSlideElement.classList.contains('video-slide');
    const initialAutoplaySpeed = isFirstSlideVideo ? 70000 : 8000;

    console.log('Initializing slider with autoplay speed:', initialAutoplaySpeed);

    $(".homepage-slider").slick({
      dots: true,
      infinite: true,
      speed: 500,
      fade: {% if settings.slider_home_transition == 'fade' %}true{% else %}false{% endif %},
      cssEase: 'linear',
      autoplay: true,
      autoplaySpeed: initialAutoplaySpeed,
      arrows: true,
      pauseOnHover: false,
      pauseOnFocus: false,
      adaptiveHeight: false,
      responsive: [
        {
            breakpoint: 480,
            settings: {
              arrows: false
            }
        }
      ]
    });

    sliderInitialized = true;

    // Handle slide changes
    $('.homepage-slider').on('beforeChange', function(event, slick, currentSlide, nextSlide) {
      stopAllVideos();

      // Pre-load the next slide video if it's a video slide
      const nextSlideElement = document.querySelector(`.slick-slide[data-slick-index="${nextSlide}"]`);
      const nextVideo = nextSlideElement ? nextSlideElement.querySelector('video') : null;
      if (nextVideo) {
        // Ensure video is fully reset for clean loading
        nextVideo.pause();
        nextVideo.currentTime = 0;
        nextVideo.load();
      }
    });

    $('.homepage-slider').on('afterChange', function(event, slick, currentSlide) {
      // Check if current slide is a video slide and adjust autoplay timing
      const currentSlideElement = document.querySelector(`.slick-slide[data-slick-index="${currentSlide}"]`);
      const isVideoSlide = currentSlideElement && currentSlideElement.querySelector('.video-slide');

      if (isVideoSlide) {
        // Video slide: set autoplay to 70 seconds (1 min 10 sec)
        console.log('Setting autoplay to 70 seconds for video slide');
        $('.homepage-slider').slick('slickSetOption', 'autoplaySpeed', 70000, true);
      } else {
        // Image slide: set autoplay to 8 seconds
        console.log('Setting autoplay to 8 seconds for image slide');
        $('.homepage-slider').slick('slickSetOption', 'autoplaySpeed', 8000, true);
      }

      // Play video with longer delay to ensure slide transition is complete
      setTimeout(() => playCurrentVideo(currentSlide), 1000);
    });

    // Play video on first slide after initialization
    setTimeout(() => {
      console.log('Playing initial slide video');
      playCurrentVideo(0);
    }, 1500);
  }

  // Initialize slider
  initializeSlider();
  setTimeout(initializeSlider, 1000);
  setTimeout(initializeSlider, 3000);
});

// Enhanced global visibility change handler to prevent video pausing
document.addEventListener('visibilitychange', function() {
  // Do nothing - let videos continue playing in background
});

// Prevent video pausing on window blur/focus
window.addEventListener('blur', function() {
  // Keep videos playing even when window loses focus
  document.querySelectorAll('.slide-video').forEach(video => {
    if (!video.ended && video.paused) {
      setTimeout(() => {
        video.play().catch(e => console.log('Resume on blur failed:', e));
      }, 100);
    }
  });
});

window.addEventListener('focus', function() {
  // Ensure videos continue playing when window gains focus
  document.querySelectorAll('.slide-video').forEach(video => {
    if (!video.ended && video.paused) {
      setTimeout(() => {
        video.play().catch(e => console.log('Resume on focus failed:', e));
      }, 100);
    }
  });
});
</script>

<script>
window.addEventListener('load', function() {
    setTimeout(function() {
        const slide1 = document.querySelector('.slide-item[data-slide-index="1"]');
        if(slide1) {
            const slide1Img = slide1.querySelector('img');
            const slide1Video = slide1.querySelector('video');

            if(slide1Img && !slide1Video) {
                slide1Img.src = 'https://cdn.shopify.com/s/files/1/1064/0118/files/Rhino_carboy_and_bottles_promo-foxx-life-sciences-home-slideshow-v2.jpg?v=1750361081';
            }
        }

        const slide2 = document.querySelector('.slide-item[data-slide-index="2"]');
        if(slide2) {
            const slide2Img = slide2.querySelector('img');
            const slide2Video = slide2.querySelector('video');

            if(slide2Img && !slide2Video) {
                slide2Img.src = 'https://cdn.shopify.com/s/files/1/1064/0118/files/Website_ISO_13485_Banner-foxx-life-sciences-HomeSlideshow.jpg?v=1750425783';
            }
        }
    }, 500);
});
</script>
